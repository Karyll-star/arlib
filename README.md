# ARLib - 智慧图书馆 AR 导览与服务系统

ARLib 是一个基于 **React Native (Expo)** 构建的现代化智慧图书馆应用，深度融合了 **AR (增强现实)** 导航与 **AI (人工智能)** 助手功能。本项目旨在通过技术手段提升读者的图书馆使用体验，包括图书检索、AR 实景导航、座位预约、社区交流以及 AI 智能问答等服务。

> **注意**: 本项目采用“宿主应用 + 功能模块”的混合开发架构。核心业务逻辑由 React Native 承载，高性能 AR 渲染规划由 Unity 引擎支持 (处于集成阶段)。

---

## 📚 核心功能 (Features)

### 1. 智慧服务
- **📚 图书管理**: `bookshelf` (我的书架)、`book-detail` (图书详情)、`favorites` (收藏夹)。
- **🪑 空间服务**: `seat-reservation` (座位预约)，支持可视化选座。
- **🔍 扫码功能**: `scan` (扫码借阅/签到)，集成相机模块。
- **🔔 消息中心**: `notifications` (借阅到期提醒、预约通知)。

### 2. AR 增强现实 (增强体验)
- **🧭 实景导航**: `ar` (AR 导航页)，提供书架与功能区的实景指引。
- **📍 定位签到**: 支持 GPS、蓝牙 Beacon 及二维码多种签到方式。
- *(注: AR 模块目前处于 React Native 侧的接口定义与 Mock 阶段，详见开发指南)*

### 3. AI 智能助手
- **🤖 智能问答**: `ai` (AI 对话页)，解答图书馆相关问题。
- **🎙️ 语音输入**: `voice-input` (语音交互)，支持语音转文字输入。

### 4. 社区互动 (思想广场)
- **💬 话题讨论**: `community` (社区首页)。
- **📝 帖子详情**: `post-detail` (阅读与评论)。

### 5. 用户中心
- **👤 个人档案**: `profile` (个人中心)。
- **⚙️ 系统设置**: `settings` (应用偏好设置)。
- **🔐 认证体系**: `sign-in` (登录/注册流程)。

---

## 🛠 技术栈 (Tech Stack)

### 核心框架
- **Runtime**: [React Native](https://reactnative.dev/) (0.74.5)
- **Framework**: [Expo SDK](https://expo.dev/) (51.0.11) - 提供托管工作流与原生能力访问。
- **Language**: [TypeScript](https://www.typescriptlang.org/) (~5.3.3) - 强类型编程语言。

### 路由与导航
- **Routing**: [Expo Router](https://docs.expo.dev/router/introduction/) (~3.5.15) - 基于文件系统的路由方案 (File-based routing)。
- **Navigation**: React Navigation (底层依赖)。

### UI 与样式
- **Styling**: [NativeWind](https://www.nativewind.dev/) (^4.0.1) - 使用 Tailwind CSS 编写 React Native 样式。
- **Design System**: Tailwind CSS (3.4.4)。
- **Icons**: @expo/vector-icons。
- **Fonts**: expo-font。

### 功能模块
- **Camera**: expo-camera - 用于扫码与 AR 预览流。
- **Animations**: react-native-reanimated - 高性能动画库。
- **Safe Area**: react-native-safe-area-context。

---

## 📂 目录结构 (Directory Structure)

```plaintext
app/
├── .expo/                  # Expo 临时配置与缓存
├── app/                    # 🚀 应用路由与页面 (Expo Router)
│   ├── (tabs)/             # 底部标签栏导航组
│   │   ├── _layout.tsx     # Tabs 布局配置
│   │   ├── index.tsx       # 首页 (Home)
│   │   ├── ar.tsx          # AR 导航入口
│   │   ├── ai.tsx          # AI 助手入口
│   │   ├── community.tsx   # 社区入口
│   │   └── profile.tsx     # 个人中心入口
│   ├── _layout.tsx         # 根布局 (Root Layout)
│   ├── book-detail.tsx     # 图书详情页
│   ├── scan.tsx            # 扫码页
│   ├── sign-in.tsx         # 登录页
│   ├── global.css          # 全局样式 (Tailwind directives)
│   └── ...                 # 其他独立页面
├── assets/                 # 静态资源 (图片、字体)
├── src/                    # 🧩 源代码 (非路由组件与逻辑)
│   ├── components/         # 可复用 UI 组件
│   ├── theme/              # 主题配置
│   ├── types.d.ts          # 类型定义
│   └── ...
├── guide/                  # 📖 项目文档与设计图
├── node_modules/           # 依赖包
├── app.json                # Expo 项目配置 (名称、版本、Scheme)
├── babel.config.js         # Babel 编译配置
├── metro.config.js         # Metro 打包配置 (适配 NativeWind)
├── tailwind.config.js      # Tailwind CSS 配置
├── tsconfig.json           # TypeScript 配置
└── package.json            # 项目依赖与脚本
```

---

## 🚀 快速开始 (Getting Started)

### 环境要求
- **Node.js**: LTS 版本 (推荐 v18+)。
- **包管理器**: npm 或 yarn。
- **移动端**: 手机安装 **Expo Go** 应用 (iOS/Android)。

### 安装步骤

1.  **克隆项目**
    ```bash
    git clone <repository-url>
    cd arlib/app0/app
    ```

2.  **安装依赖**
    ```bash
    npm install
    # 或者
    yarn install
    ```

3.  **启动开发服务器**
    ```bash
    npm start
    # 或者
    npx expo start
    ```

4.  **运行应用**
    - 终端将显示一个 QR Code。
    - 使用手机上的 **Expo Go** 扫描该二维码。
    - 按 `a` 在 Android 模拟器运行，按 `i` 在 iOS 模拟器运行，按 `w` 在 Web 浏览器运行。

---

## 📜 可用脚本 (Scripts)

在 `package.json` 中定义了以下常用命令：

| 命令 | 说明 |
| :--- | :--- |
| `npm start` | 启动 Metro Bundler 服务器 (默认) |
| `npm run android` | 启动并尝试在 Android 模拟器/设备上运行 |
| `npm run ios` | 启动并尝试在 iOS 模拟器上运行 (仅 macOS) |
| `npm run web` | 在浏览器中以 Web 应用模式运行 |

---

## 🚧 开发指南与规范

### 1. 样式开发 (Styling)
本项目使用 **NativeWind**。请直接在组件的 `className` 属性中编写 Tailwind 类名。
```tsx
<View className="flex-1 bg-white items-center justify-center">
  <Text className="text-lg font-bold text-gray-800">Hello ARLib</Text>
</View>
```

### 2. 路由跳转 (Routing)
使用 `expo-router` 的 `Link` 组件或 `router` 对象进行导航。
```tsx
import { Link, router } from 'expo-router';

// 组件方式
<Link href="/book-detail">Go to Book</Link>

// 编程式
router.push('/scan');
```

### 3. AR 模块开发策略
目前 AR 功能采用 **Mock (模拟)** 策略开发。
- 所有的 AR 交互界面在 `app/ar.tsx` 或相关组件中通过 React Native 原生视图进行模拟。
- 真实的 Unity 集成将在后续阶段通过 `react-native-unity` 进行桥接 (详见根目录 `guide.md`)。

---

## 🤝 贡献 (Contributing)

欢迎提交 Issue 或 Pull Request。在提交代码前，请确保：
1.  代码通过 TypeScript 类型检查。
2.  组件样式在 iOS 和 Android 上表现一致。

## 📄 许可证 (License)

[MIT License](LICENSE)
